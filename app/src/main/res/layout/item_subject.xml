<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mathsCard"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:elevation="20dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/mathsCardView"
        android:layout_width="70dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:padding="5dp"
        app:cardCornerRadius="15dp"
        app:cardElevation="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/imgSubject"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="10dp"
            android:contentDescription="@string/todo"
            android:src="@drawable/image_maths"
            android:scaleType="centerCrop"
            android:adjustViewBounds="true"
            app:layout_constraintDimensionRatio="1:1" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/nameSubject"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="?attr/colorOnBackground"
        android:textSize="12sp"
        android:textStyle="bold"
        android:text="@string/maths"
        android:maxLines="2"
        android:minHeight="32dp"
        app:layout_constraintTop_toBottomOf="@id/mathsCardView"
        app:layout_constraintStart_toStartOf="@id/mathsCardView"
        app:layout_constraintEnd_toEndOf="@id/mathsCardView" />

</androidx.constraintlayout.widget.ConstraintLayout>